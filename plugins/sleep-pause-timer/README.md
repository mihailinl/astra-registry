# Sleep Pause Timer

Плагин [Astra](https://github.com/mihailinl/AstraPlugins) от **Voltur** (лицензия MIT) —
портирование автономной программы «Таймер паузы» (`sleep_pause.py`).

## Что умеет

- **Обратный отсчёт** с паузой, продолжением и отменой — управляется голосом
  или текстом через Астру.
- **Будильник** на заданное время с воспроизведением локального аудиофайла
  (MP3, WAV, WMA, M4A, AAC, MIDI). Однократный или по выбранным дням недели.
- **Календарь будильников** — разовые сигналы на любые даты ближайших месяцев;
  расписание сохраняется локально.
- **Отслеживание бездействия Windows** с настраиваемым порогом и действием.
- **Действие по завершении таймера** (как в оригинальной программе):
  - `playpause` — нажать медиа-клавишу play/pause (пауза видео/музыки);
  - `shutdown` — выключить ПК;
  - `sleep` — усыпить ПК.
- **Активация окна** перед нажатием клавиши: по подстроке заголовка или по
  номеру `#N` из списка окон (поддержка Chromium-браузеров — Яндекс.Браузер,
  Chrome, Edge — как в оригинале).
- **Виджеты на главном экране** Astra (слот `home.widgets`), одного размера:
  - Таймер: отсчёт, прогресс, кнопки «пауза/продолжить» и «отмена».
  - Будильник: отсчёт до звонка, время и мелодия, «отмена»; во время звонка —
    «стоп» (заглушить) и «отмена». Время и мелодия ставятся инструментами
    (`set_alarm`, `choose_alarm_sound`) или в настройках плагина.
  Оба прозрачные, следуют теме Astra.
- Отдельная страница **«Таймер паузы»** в навигации Astra: таймер, расписание,
  календарь, окна и локальные настройки.

## Инструменты для Астры

| Инструмент | Что делает |
|---|---|
| `start_timer` | Запустить отсчёт: `hours`, `minutes`, `seconds`, `action`, `window` |
| `pause_timer` | Пауза |
| `resume_timer` | Продолжить |
| `cancel_timer` | Отменить (действие не выполняется) |
| `timer_status` | Сколько осталось и что будет по завершении |
| `list_windows` | Нумерованный список открытых окон для выбора цели |
| `set_alarm` | Установить будильник: `time` (`ЧЧ:ММ`), `sound_path`, `repeat`, `interval`, `days_of_week` (0=Пн … 6=Вс) |
| `set_alarm_sound` | Задать мелодию будильника (путь к файлу) |
| `alarm_status` | Статус будильника: время, повтор, мелодия |
| `cancel_alarm` | Отменить будильник |
| `stop_alarm_sound` | Остановить воспроизведение будильника |
| `choose_alarm_sound` | Открыть системный диалог выбора аудиофайла |
| `set_monthly_alarm` / `cancel_monthly_alarm` | Добавить или удалить разовый сигнал на дату `ГГГГ-ММ-ДД` |
| `calendar_alarm_status` | Показать предстоящие сигналы календаря |
| `start_idle_monitor` / `resume_idle_monitor` / `stop_idle_monitor` / `idle_status` | Запуск, перезапуск отсчёта с текущего момента, остановка и проверка бездействия |

Примеры: *«поставь таймер на 5 минут, потом нажми play в YouTube»*,
*«таймер на полчаса и выключи компьютер»*, *«сколько осталось?»*,
*«разбуди меня в 7:30 под мою мелодию»*, *«отмени будильник»*.

## Настройки

Действие по умолчанию, окно и параметры бездействия задаются во вкладке
плагина и сохраняются в `%APPDATA%\sleep-pause-timer\settings.json`.
Будильник хранится в `alarm.json`, календарь — в `calendar.json` в этой папке.

## Разрешения

Плагин **не запрашивает ни одного разрешения**: виджеты сами опрашивают плагин,
инструменты вызывает Астра, наружные вызовы демону не нужны.

## Установка (режим разработчика)

1. В Astra: **Settings → Privacy** → включить **«Allow unsigned plugins»**.
2. **Plugins → Dev** → вставить путь к папке в поле **«Load a plugin»** → нажать загрузку.
3. После перезапуска Astra плагин из папки возвращается **выключенным** —
   включите его во вкладке Installed.

Собственное виртуальное окружение (`.venv`) лежит внутри папки плагина —
системный Python не используется.

## Отличия от оригинальной программы

- Не нужны pyautogui/pygetwindow/win32gui/pystray — всё на чистом `ctypes`.
- Исправлен спящий режим: `SetSuspendState` вызывается из `PowrProf.dll`
  (в оригинале вызов через `kernel32` падал с `AttributeError`).
- GUI на Tkinter заменён виджетами в окне Astra; трей не нужен — управляет Астра.

## Development

Language: python

```bash
astra-plugin dev .
```

## Build

```bash
astra-plugin build
```

This produces a `.astraplugin` file for distribution.

## Лицензия

MIT © Voltur

## Поддержать автора

Если плагин оказался полезным, можно поддержать разработку:

**[boosty.to/voltur/donate](https://boosty.to/voltur/donate)**
